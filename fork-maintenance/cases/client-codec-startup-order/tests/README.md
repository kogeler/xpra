The retained regression lives in `fix.patch` at
`tests/unittests/unit/client/subsystem/codec_startup_test.py`, including its
native child-process control. No duplicate acceptance probe is maintained here.
Use the named focused modes and complete-stack gates in the case README.
