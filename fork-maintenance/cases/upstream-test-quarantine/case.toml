schema = 1
# Permanent inactive infrastructure, not a production fix. Never delete this case.
# Enable only to disable currently broken upstream tests, after clean-source proof.
# workspace-update removes the draft marker and derives the active patch fields.
draft = true
slug = "upstream-test-quarantine"
kind = "test-quarantine"
title = "Quarantine failing upstream test modules"
commit_subject = "Quarantine failing upstream test modules"
# Empty draft placeholders: fix.patch is zero bytes and is not selectable.
patch_sha256 = ""
dependencies = []
paths = []

[tests]
list = [
  # Add the current unit.* module union before promotion, then enable these legs:
  # "full",
  # "full-cython",
  # "full-no-compat",
]

[quarantine]
modules = []

[quarantine.gates]
quarantine = []
quarantine-cython = []
quarantine-no-compat = []

[evidence]
required_gates = [
  # Enable all three only when quarantining currently broken upstream tests:
  # "quarantine",
  # "quarantine-cython",
  # "quarantine-no-compat",
]
