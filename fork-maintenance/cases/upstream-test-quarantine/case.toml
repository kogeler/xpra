schema = 2
# Permanent infrastructure, not a production fix. Never delete this case.
# Inactive: no Fork-Case commit and every list below empty (not selectable).
# Activate only to quarantine currently broken upstream tests after clean-source
# proof: list the modules, their gates and tests, then add one
# `Fork-Case: upstream-test-quarantine` commit changing exactly those files.
slug = "upstream-test-quarantine"
kind = "test-quarantine"
title = "Quarantine failing upstream test modules"
dependencies = []

[tests]
list = [
  # Add the current unit.* module union before activation, then enable these legs:
  # "full",
  # "full-cython",
  # "full-no-compat",
]

[quarantine]
modules = []

[quarantine.gates]
quarantine = []
quarantine-cython = []
quarantine-no-compat = []

[evidence]
required_gates = [
  # Enable all three only when quarantining currently broken upstream tests:
  # "quarantine",
  # "quarantine-cython",
  # "quarantine-no-compat",
]
