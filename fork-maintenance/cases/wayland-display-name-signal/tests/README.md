The retained regression is part of `fix.patch` at
`tests/unittests/unit/wayland/manager_signal_test.py`. It exercises the actual
manager, signal emitter and native compositor. No additional probe is stored
in this directory. Run it through the case's named focused modes and the
complete-stack validation flow documented in the case README.
